<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vivardhan.attendanceedit">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo722"
        android:label="Attendance"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_CSE"
            android:label="@string/csea3">

        </activity>
        <activity android:name=".AcitivityAbsentees"
                  android:label="Absentees III CSE A"
                  android:theme="@style/AppTheme"/>
        <activity android:name=".studentDetails"
                  android:label="Edit Details"
                  android:theme="@style/AppTheme"/>
        <activity android:name=".DeleteStudent"
                  android:label="Delete"
                  android:theme="@style/AppTheme"/>
        <activity android:name=".EditDetails"
                  android:label="Edit"
                  android:theme="@style/AppTheme"/>
    </application>

</manifest>
